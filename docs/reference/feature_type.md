# This function is used to return the type(numeric,binary,multiclass) of each feature

This function is used to return the type(numeric,binary,multiclass) of
each feature

## Usage

``` r
feature_type(data)
```

## Arguments

- data:

  A data frame, tibble, or data table.
