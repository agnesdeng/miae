# postprocess data

postprocess data

## Usage

``` r
postprocess(output.data, pre.obj, scaler)
```

## Arguments

- output.data:

  raw output data from autoencoders

- pre.obj:

  preprocess data object

- scaler:

  scaler used to scale the data
