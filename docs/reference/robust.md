# scale a vector using robust scaler

scale a vector using robust scaler

## Usage

``` r
robust(x, lower = 0.25, upper = 0.75)
```

## Arguments

- x:

  a vector

- lower:

  The lower quantile (25% by default)

- upper:

  The upper quantile (75% by default)
